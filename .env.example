# Base database settings
POSTGRES_USER=n8n
POSTGRES_PASSWORD=n8n
POSTGRES_DB=n8n
TZ=Europe/Kyiv

# Public URLs (set to your Cloudflare tunnel hostname)
N8N_WEBHOOK_URL=https://CHANGE_ME_DOMAIN
N8N_EDITOR_BASE_URL=https://CHANGE_ME_DOMAIN
WEBHOOK_URL=https://CHANGE_ME_DOMAIN

# n8n application secret (deploy.ps1 will replace if left as placeholder)
ENCRYPTION_KEY=CHANGE_ME_GENERATED

# Cloudflare Tunnel token (do NOT commit a real token)
CLOUDFLARED_TOKEN=CHANGE_ME_TUNNEL_TOKEN

# Telegram bot token (BotFather) - keep secret locally
TELEGRAM_TOKEN=CHANGE_ME_TELEGRAM_TOKEN

# Trust all proxies (adjust if you want to narrow ranges)
N8N_TRUSTED_PROXIES=0.0.0.0/0,::/0

# Optional performance/features (uncomment to enable)
# N8N_RUNNERS_ENABLED=true
# N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true

# Notes:
# 1. Copy this file to .env and replace CHANGE_ME values before first deploy.
# 2. Never commit the real .env to Git. Keep only this template.
# 3. deploy.ps1 will generate a random ENCRYPTION_KEY if you keep the placeholder above.
